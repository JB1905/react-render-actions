import { Children } from '../interfaces/Children';

interface Props extends Children {}

export const Promise = ({ children }: Props) => {
  // const promise = new Promise((resolve, reject) => {});
  // promise
  //   .then(() => {
  //     return children;
  //   })
  //   .catch(() => {
  //     return children;
  //   });
};
