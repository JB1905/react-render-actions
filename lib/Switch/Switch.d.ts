import { Children } from '../interfaces/Children';
interface Props extends Children {
    switchValue: any;
}
export declare const Switch: ({ switchValue, children }: Props) => JSX.Element;
export {};
