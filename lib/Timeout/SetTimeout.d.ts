interface Props {
    children: any;
    callback: () => void;
    enabled: boolean;
    timeout: number;
}
export declare const SetTimeout: ({ children, enabled, timeout }: Props) => any;
export {};
