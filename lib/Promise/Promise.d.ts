interface Props {
    children: any[];
    promise: Promise<any>;
    type: any;
    fallback: any;
}
export declare const RPromise: ({ children, promise, type, fallback }: Props) => void;
export {};
